# E2E 测试环境变量
# 复制此文件为 .env 并填写实际值：cp .env.example .env

# 被测应用地址（必填）
E2E_BASE_URL=https://your-server-address

# 登录账号
E2E_USERNAME=your-username
E2E_PASSWORD=your-password

# 是否无头模式（默认 true，设为 false 可看到浏览器）
E2E_HEADLESS=true

# 验证码 OCR 最大重试次数（默认 10）
E2E_MAX_OCR_ATTEMPTS=10
