---
description: 重新生成文档站参考页（agents/skills/commands/rules/changelog）
---

## 你的任务

重新生成文档站 `reference/` 目录下的自动生成页面。

### 操作步骤

```bash
cd docs && npm run docs:generate-ref && npm run docs:build
```

确认构建无报错即可。**不要手动编辑** `reference/` 下的文件。
